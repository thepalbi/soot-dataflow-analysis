package analysis;

/**
 * Common methods used for marking a variable as <b>sensible</b>, or <b>sanitizing</b> it.
 */
public class SensibilityMarker {

    /**
     * Marks the supplied {@link Object} as sensible.
     *
     * @param someObject the object to mark
     */
    public static void markAsSensible(Object someObject) {
        // Do nothing.
    }

    /**
     * Clears the supplied {@link Object} sensibility, ergo, sanitizing it.
     *
     * @param someObject the object to mark
     */
    public static void sanitize(Object someObject) {
        // Do nothing.
    }
}
